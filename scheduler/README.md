# Scheduler Service

This repository contains the Scheduler service. Contributor-oriented architecture and developer notes are moved to a dedicated file to reduce duplication with other services.

See the contributor-oriented material in: [scheduler/ARCHITECTURE.md](scheduler/ARCHITECTURE.md)

Operational/API usage and setup details are deferred to a follow-up docs issue to avoid duplication across services.

If you need quick operational references, inspect `tasks.py`, `main.py`, and `logger.py`.
