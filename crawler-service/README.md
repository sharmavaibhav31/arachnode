# Crawler Service

This repository contains the crawler (Scrapy) service. Contributor-oriented architecture and developer notes are moved to a dedicated file to reduce duplication with other services.

See the contributor-oriented material in: [crawler-service/ARCHITECTURE.md](crawler-service/ARCHITECTURE.md)

Operational/API usage and setup details are deferred to a follow-up docs issue to avoid duplication across services.

If you need quick operational references, inspect `crawler/` package, `scrapy.cfg`, and `read_stream.py`.
